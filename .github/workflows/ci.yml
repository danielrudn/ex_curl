name: CI
on:
  pull_request:
  push:
    branches:
      - master

jobs:
  ci:
    runs-on: ubuntu-20.04
    env:
      MIX_ENV: test
      KERBEROS_USERNAME: admin
      KERBEROS_PASSWORD: secretpassword
    strategy:
      fail-fast: false
      matrix:
        include:
          - pair:
              elixir: "1.11.4"
              otp: "23.0"
          - pair:
              elixir: "1.14"
              otp: "25.0"
            lint: lint
    steps:
      - uses: actions/checkout@v2

      - name: Install test dependencies
        run: sudo apt-get update && sudo apt-get install -y libcurl4-openssl-dev krb5-{user,kdc,admin-server}

      - name: Setup Kerberos environment and initialize configuration
        run: sudo ./test/support/files/setup_kerberos.sh

      - name: Create Kerberos ticket for test user
        run: |
          cat /etc/hosts
          export KERBEROS_REALM=$(cat /etc/hostname | cut -d'.' -f2,3)
          kinit -k -t /etc/krb5.keytab "HTTP/localhost@${KERBEROS_REALM^^}"
          klist

      - uses: erlef/setup-beam@main
        with:
          otp-version: ${{matrix.pair.otp}}
          elixir-version: ${{matrix.pair.elixir}}
          version-type: strict

      - uses: actions/cache@v2
        with:
          path: deps
          key: mix-deps-${{ hashFiles('**/mix.lock') }}

      - run: mix deps.get

      - run: mix format --check-formatted
        if: ${{ matrix.lint }}

      - run: mix deps.unlock --check-unused
        if: ${{ matrix.lint }}

      - run: mix deps.compile

      - run: mix compile --warnings-as-errors
        if: ${{ matrix.lint }}

      - run: mix test --include kerberos:true
        if: ${{ ! matrix.lint }}

      - run: mix test --warnings-as-errors --include kerberos:true
        if: ${{ matrix.lint }}
